/**
 * bootstrap.js — DSLE
 *
 * Punto de arranque de la capa JavaScript. Aquí se inicializan utilidades
 * globales del cliente (por ejemplo, configuración de cabeceras para
 * peticiones a la API). Los módulos específicos por rol se cargan desde
 * resources/js/modules/ en la vista que los necesite.
 */

// Ejemplo de configuración futura (se activará en su fase):
// import axios from 'axios';
// window.axios = axios;
// window.axios.defaults.headers.common['X-Requested-With'] = 'XMLHttpRequest';
